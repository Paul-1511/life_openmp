#include <omp.h>    // Incluye la librería OpenMP para soporte de programación paralela

// Define constantes para representar el estado de las células
#define LIVE 1   // Representa una célula viva
#define DEAD 0   // Representa una célula muerta

// Declaración de la función `hash`, que probablemente se utiliza para 
// generar un valor hash o algún tipo de código basado en ciertos datos
int hash();

// Declaración de la función `chunk`, que probablemente se utiliza para
// dividir datos en partes más pequeñas (chunks) o para manejar 
// porciones de datos en paralelo
int chunk();
